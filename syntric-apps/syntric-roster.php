<?php
/**
 * Roster bock template
 *
 * @param array  $block      Block settings and attributes
 * @param string $content    Block inner HTML (empty)
 * @param bool   $is_preview True during AJAX preview
 * @param (int/string) $post_id     ID of the post the block is saved to
 */

echo 'this is syntric-roster.php';

var_dump( $block );
var_dump( $content );
var_dump( $is_preview );
var_dump( $post_id );
